name: CD - Deploy (runtime only)

on:
<<<<<<< Updated upstream
=======
  workflow_dispatch:
    inputs:
      mode:
        description: "resume (restart only) or update (pull + sbt + build + up)"
        required: true
        default: "resume"
        type: choice
        options:
          - resume
          - update
>>>>>>> Stashed changes
  workflow_run:
    workflows:
      - CI - sbt assembly & Docker build
    types:
      - completed

jobs:
  deploy:
<<<<<<< Updated upstream
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    runs-on: [self-hosted, deploy-vm]   # ← run on your VM (self-hosted runner with label deploy-vm)

    permissions:
      contents: read
      packages: read
=======
    if: ${{ (github.event_name == 'workflow_run' && github.event.workflow_run.conclusion == 'success' && github.event.workflow_run.head_branch == 'main') || (github.event_name == 'workflow_dispatch') }}
    runs-on: ubuntu-latest
>>>>>>> Stashed changes

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Check runner prerequisites 🔍
        run: |
          echo "Checking Docker & Docker Compose versions..."
          docker --version
          docker compose version

      - name: Write .env from secrets (on VM) 🔐
        if: ${{ secrets.POSTGRES_PASSWORD }}
        run: |
          cat > .env <<EOF
          POSTGRES_DB=${{ secrets.POSTGRES_DB }}
          POSTGRES_USER=${{ secrets.POSTGRES_USER }}
          POSTGRES_PASSWORD=${{ secrets.POSTGRES_PASSWORD }}
          PGADMIN_EMAIL=${{ secrets.PGADMIN_EMAIL }}
          PGADMIN_PASSWORD=${{ secrets.PGADMIN_PASSWORD }}
          EOF
          echo ".env written (secrets redacted in logs)"

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set runtime variables
        run: |
          OWNER=$(echo "${{ github.repository_owner }}" | tr '[:upper:]' '[:lower:]')
          REPO=$(basename "${{ github.repository }}" | tr '[:upper:]' '[:lower:]')

          echo "OWNER=$OWNER" >> $GITHUB_ENV
          echo "REPO=$REPO" >> $GITHUB_ENV
          echo "APP_TAG=main" >> $GITHUB_ENV

      - name: Pull images
        run: |
          docker pull ghcr.io/$OWNER/$REPO/producer:$APP_TAG
          docker pull ghcr.io/$OWNER/$REPO/consumer:$APP_TAG

      - name: Deploy containers (NO BUILD)
        run: |
          docker compose \
            -f docker-compose.prod.yml \
            up -d --no-build

      - name: Check status
        run: |
          docker compose -f docker-compose.prod.yml ps
          docker images | grep ghcr.io || true
